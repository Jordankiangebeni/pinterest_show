{% extends 'base.html.twig' %}

{% block title %}All the best pins on Earth {% endblock%}
{% block body %}
   <article>
    <h1>{{pin.title}}</h1>
    <p>{{pin.description}}</p>
   </article>
<a href="{{ path('app_pins_edit', {id : pin.id}) }}"> Edit</a> |
<a href="{{ path('app_pins_delete', {id : pin.id}) }}" onclick="event.preventDefault(); document.getElementById("js-pin-delete-form").submit();">delete</a>|
     <a href="{{path("app_home")}}"></a>
  <form id="js-pin-delete-form" action="{{ path('app_pins_delete', {id : pin.id}) }}" method="post" style="display:none;">
   <input type="hidden" name="_method" value="DELETE"> 
  </form>
{% endblock %}   
